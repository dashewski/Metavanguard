import { ethers } from "ethers"
import { UsersMarketplace__factory } from "../typechain-types";

async function main() {}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});